όνομα: Σκανερ Ρουκέτας (κάθε 4 ώρες)

επί:

πρόγραμμα:

- cron: '30 3,7,11,15,19,23 * * *'

ροή εργασίας_αποστολή:

θέσεις εργασίας:

σάρωση:

τρέχει σε: ubuntu-latest

βήματα:

- όνομα: Αποθετήριο ολοκλήρωσης αγοράς

χρήσεις: ενέργειες/checkout@v4

- όνομα: Ρύθμιση Python

χρήσεις: actions/setup-python@v5

με:

έκδοση python: '3.11'

- όνομα: Εξαρτήσεις εγκατάστασης

εκτέλεση: αιτήματα εγκατάστασης pip

- όνομα: Εκτέλεση σαρωτή

περιβάλλον:

TELEGRAM_TOKEN: ${{ μυστικά.TELEGRAM_TOKEN }}

TELEGRAM_CHAT_ID: ${{ μυστικά.TELEGRAM_CHAT_ID }}

εκτέλεση: python scanner.py

